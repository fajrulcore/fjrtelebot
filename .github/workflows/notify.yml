name: Cron Test

on:
  schedule:
    - cron: "*/2 * * * *"
  workflow_dispatch:

jobs:
  cron-test:
    runs-on: ubuntu-latest
    steps:
      - name: Print current time
        run: echo "🕒 Cron triggered at $(date)"
